name: Build and Run Integration Tests

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  integration-test:
    runs-on: ubuntu-latest
    container:
      image: debian:bookworm-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y curl build-essential gcc pkg-config libssl-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.93.0"

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Start mock target
        run: cargo run -p integration-tests -- mock-target

      - name: Start proxy
        run: cargo run --package proxy --bin proxy -- --config config/local/proxy.toml

      - name: Start agent
        run: cargo run --package agent --bin agent -- --config config/local/agent.toml

      - name: Integration Tests
        run: cargo run --package integration-tests --bin integration-tests -- integration --agent-addr 127.0.0.1:7070 --proxy-addr 127.0.0.1:8080
