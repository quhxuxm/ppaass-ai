[build]
# `aes_armv8` lets RustCrypto select ARMv8 AES instructions at runtime on
# aarch64 macOS/Android. It is ignored by the AES backend on other targets.
rustflags = ["--cfg", "tokio_unstable", "--cfg", "aes_armv8"]

[target.x86_64-unknown-linux-musl]
linker = "rust-lld"
